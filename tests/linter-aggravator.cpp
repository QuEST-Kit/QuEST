#include "quest.h"

int main() {

    // frankly, the existing source code is 
    // likely stanky enough that the linter 
    // will lose its mind; I flatter myself
    // to imply I must deliberately provoke it.
    return 0;
}
